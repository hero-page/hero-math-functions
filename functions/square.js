/* eslint-disable */ 

/**
 * Squares a number.
 *
 * @param {number} num - The number to be squared.
 * @return {number} The squared result of the input number.
 * @example
 * // returns 16
 * square(4)
 */
function square(num) {
    return Math.pow(num, 2);
}

module.exports = {
    square
};